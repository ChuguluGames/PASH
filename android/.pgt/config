# # custom builder script before compilation (set to "no" to disable)
BUILDER=android/.pgt/builder
# directory of the webapp relative to the app root
WWW_DIR=brunch/android/
# final phonegap library path in the android app assets
PHONEGAP_PATH=js/
